<h1>Listing users</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Password hash</th>
    <th>Password salt</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user._id %></td>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
